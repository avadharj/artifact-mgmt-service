#!/bin/sh

# Use Java 11 so Gradle's Groovy DSL can run (Java 25 is incompatible with Gradle 8.x DSL).
JAVA_11=$(/usr/libexec/java_home -v 11 2>/dev/null || true)
if [ -n "$JAVA_11" ]; then
  export JAVA_HOME="$JAVA_11"
fi

cd "$(git rev-parse --show-toplevel)"
npx lint-staged
